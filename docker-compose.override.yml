# Local development override for Docker Compose
# This file overrides the default port to avoid conflicts

version: '3.7'

services:
  web:
    ports:
      - "5001:5000"  # Map external port 5001 to internal port 5000
    environment:
      FLASK_ENV: development
      FLASK_DEBUG: 1